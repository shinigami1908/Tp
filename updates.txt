def _derive_safe_ratios(merged: pd.DataFrame) -> pd.DataFrame:
    """
    No-op placeholder for derived ratios.

    You asked to drop 'completion_ratio' and similar derived KPIs.
    Keeping this function as a no-op avoids KeyError and keeps
    call sites intact (we still call it, but it will not create new columns).
    """
    return merged
